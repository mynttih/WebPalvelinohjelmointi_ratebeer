<%= bootstrap_form_with(model: beer, local: true) do |form| %>
  <% if beer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
      <% beer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.select :style, options_for_select(@styles.collect { |style| [style.name, style.id] }) %>
  </div>

  <div class="field">
    <%= form.select :brewery_id, options_from_collection_for_select(@breweries, :id, :to_s) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
